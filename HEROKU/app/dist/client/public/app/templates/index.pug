html(lang="en", ng-app="app", layout-fill ="")
  include layout/head

  script().
    var participants = !{JSON.stringify(participants)};

    window.participants = participants;

  body(layout="column", ng-cloak="", ng-controller="MainController as mainController", ng-show="::mainController.isReady",
  layout-fill="")
    //include layout/toolbar
    div(flex="", layout="row")
      //include layout/sidenav
      include layout/content
    include layout/scripts
